# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.env
dist
dist-syndicate
dist-ssr
*.local

# Supabase CLI / slot sync — keep test & prod service keys out of git
.env.supabase.test
.env.supabase.production

# Machine Pro local export (member content — never commit)
.machinepro-auth.json
machinepro-export/

# Advantage Slots local export (member content — never commit)
.advantageslots-auth.json
advantageslots-export/

# AdvantagePlay.club local export (member content — never commit)
.advantageplay-auth.json
advantageplay-export/

# SlotFarmers.club local export (member content — never commit)
.slotfarmers-auth.json
slotfarmers-export/
ap-guide-workspace/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/css-custom-data.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

supabase/.temp/
supabase/.branches/

# iOS / Xcode
ios/EdgeTilt.xcodeproj/xcuserdata/
ios/EdgeTilt.xcodeproj/project.xcworkspace/xcuserdata/
**/xcuserdata/
DerivedData/
*.xcuserstate

# Cursor chat export state (stale � scripts removed 2026-05-25)
.cursor/chat-export-state-*.json

# Local Lounge video fixtures / probe artifacts (never commit)
IMG_1425.MOV
.tmp-*.mp4
pokerbankrollcalc.mp4
ScreenRecording_*.mp4
tmp-poker-frames/
tmp-poker-overview-frames/
tmp-poker-overview-dd/
tmp-ffmpeg-tools/
.tmp-img-cmp/
tmp-ipa-cmp/

# Agent / debugging scratch: one-off probes, generated migration chunks, vendor
# bundles pulled down to read, screen recordings. ESLint already ignores these;
# git needs to as well, or a careless `git add -A` commits them. Archived out to
# ../LVSlotPro-archive/ on 2026-08-27 rather than deleted.
.tmp-*
tmp-*
scripts/.tmp-*
scripts/tmp-*
test_files/
assets/lightbox-ref-frames/
assets/lightbox-close-ref-frames/
