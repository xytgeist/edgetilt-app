# IGT Must Hit By — advantage play notes (stub)

_Generated seed — edit freely. Sync to Supabase with `npm run slots:sync` when ready._

## What you are hunting

Folder slug **`must-hit-by-igt`** aligns with **`machine.slug`** for sync. The app’s bundled demo historically used **`igt-must-hit-by`**; unify slugs deliberately if you want one DB row covering both conventions.

Document **eligible bets**, displayed **must-hit ladders**, increment behavior observed on-location, and any **bonus vs base-game** quirks for each title you scout.

## +EV checklist (fill in)

- Photo marquee / progressive captions with denomination context.
- Note **worst credible loss** scenarios before sizing action.

## Asset note

Bundled **`IGT_Main_Image.png`** (and supplemental JPGs here) → promote to CDN `thumbnail_url` / hero URLs when publishing.

## Nerf / marketing

Assume **marketing graphics** can lag refreshed progressive caps — refresh trip notes routinely.
