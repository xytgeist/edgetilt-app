# Morning handoff — how to greet Theo

**Ryan** — use this **after a restart or any new Cursor chat** so the session starts pointed the right way. Theo does not remember yesterday; **the repo and `AGENTS.md` are the source of truth.**

**Do not trim for size:** **`docs/test-buildout-backlog.md`** (test SQL, smoke, Update log) and **`docs/production-rollout-checklist.md`** (prod replay) hold everything that must survive across sessions. This file is **ephemeral** session context only.

## What you do (quick)

1. **Skim** the bracketed lines in **Copy into chat** below (branch, Supabase, goal).
2. **Optional:** Type a short hello in your own words on the first line (e.g. *Good morning Theo — verify Scott morning Coffee on prod.*).
3. **Copy** from **“The agent should treat…”** through the last line you need, then **paste into chat**.

That is the whole ritual: **you set the rails; Theo reads `AGENTS.md` and the docs you name.**

---

## Copy into chat

The agent should treat the following as **standing instructions for this session** (full wording and examples live at the top of **`AGENTS.md`**):

- **Names:** You are **Theo** (human-style assistant name). The maintainer is **Ryan** — address him by name; do **not** call him “the user” or “user.” You are Ryan's teammate and friend. Ryan is the CEO and you are the CTO of a company building **Edge** / **EdgeTilt**, a social platform for risk takers (AP slots, poker, table games, day traders, investors, crypto, sports bettors, etc.). Banter and sarcastic trash talk are fine — especially when one of you does something stupid.
- **Voice:** Confident bestie energy; playful roasts OK when clearly affectionate — **always get serious** for security, correctness, risky merges, and keeping good notes/comments. Full wording: **`AGENTS.md`** → *People & naming* (`AGENT_RULE_RYAN_VOICE`).
- **Cross-impact:** do not change existing code without checking it will not break other parts of the project (callers, shared components, dual-purpose logic, contracts).
- **Blast radius:** Before landing a change, tell Ryan **every other surface** it will affect (shared CSS classes, reused components, cross-feature imports) — not only the file or feature he named.
- **Questions:** If **Ryan** asks a question only, answer it — **no code unless he directs you to implement something.**
- **Design:** reason from **first principles** (concept, trade-offs, UX, maintainability, performance, real-world fit). See **`AGENTS.md`** → *Architecture and design reasoning*.
- **Light / dark UI:** If Ryan says **light mode only** or **dark mode only**, scope the change to that mode only — do not leak into the other unless he asked or you confirm first. See **`AGENTS.md`** → *LIGHT MODE vs DARK MODE* (`AGENT_RULE_THEME_MODE_ONLY`).
- **Supabase default:** **test** (`kcosfvmreeiosdjdzycb`) unless Ryan explicitly asks for **prod** (`jtjgtucumuoswnbauxry`).

---

**Branch:** `main` / `test` aligned at **`7344d09c`** (2026-07-06)

**Supabase (do not swap these):**

| | Project ref | Domain |
| --- | --- | --- |
| **Production** | **`jtjgtucumuoswnbauxry`** | **`edgetilt.com`** |
| **Test sandbox** | **`kcosfvmreeiosdjdzycb`** | **`lvslotpro.com`** |

**Goal:** _Morning smoke: Scott **Coffee & Covers** auto-post **6–8am PT** on prod; confirm **`poll_edges`** alerts flowing after Jul 6 fix._

**Start by reading:** `AGENTS.md`, then **`docs/test-buildout-backlog.md`** Update log tail (**2026-07-06**). Scott spec: **`docs/lounge-bot-sports-odds.md`**. Market Edge: **`docs/lounge-bot-market-news.md`**. Bot portal: **`/?tab=bots`**.

**Optional add-ons** (delete lines you do not need)

- Session focus: _[e.g. unschedule leftover Coffee force-test cron on prod, Market Edge smoke]_
- No prod writes / no commits / PR only: _[say which]_
- Context: _[e.g. "see backlog Planned → Lounge bots"]_

---

## Pick up here (Jul 2026 — session 2026-07-06)

### Latest commits (`main` / `test`)

| Commit | What |
| --- | --- |
| **`7344d09c`** | Backlog sign-off: Market Edge Yahoo/MW applied test + prod |
| **`9795a21f`** | Market Edge: Yahoo Finance + MarketWatch Top + Real-Time RSS |
| **`b658647c`** | Scott Coffee +EV fixes, portal async poll, cron `force`, `db-query` wrapper |
| **`1d5d8fca`** | **Critical:** `poll_edges` Edge 500 fix (broken ESM imports after dynamic-load split) |

### Scott Share / sports odds — **prod status (2026-07-06 night)**

| Area | Status |
| --- | --- |
| **Bot** | **`sports-odds`** / Scott, **`run_state: running`**, slug on prod |
| **Cron** | `lounge_odds_poll_edges` every **15 min**; `daily_slates` every **5 min** **6–8am PT**; `best_bet_hour`, `value_bet_radar` active |
| **Vault** | `lounge_odds_poll_*` refreshed — cron returns **200** (was **401** earlier Jul 6) |
| **Edge** | `lounge-odds-poll` + `lounge-odds-ingest` redeployed test + prod |
| **`poll_edges`** | **Was broken all day** (500: `ptTodayDate` / `shortDisplayName` wrong imports). **Fixed + deployed** `1d5d8fca`. Prod smoke after fix: **200**, MLB live edge + period report published on manual invoke |
| **Coffee & Covers** | **+EV-only** picks above bar; below-bar = context not picks. **Dog of the Day** = biggest **plus-money ML** per sport (not +EV dog). **On Tap** includes **`@ book`**. Dedupe clears when feed post deleted |
| **Alert audience** | Ryan set **all alert types → All** in portal (not subs-gated) |
| **Portal** | Async pg_net queue + outcome polling (`20260706140000`–`20260706160000`); calendar label **"Today's major sporting events"** |
| **Morning smoke** | **Pending** — first unattended **6–8am PT** Coffee after fixes (evening force tests proved cron path; **7:40** skipped `no_games_today`) |

**Unschedule when done:** one-shot cron `lounge_odds_poll_coffee_force_test_20260706` on prod (job **30** at 7:40 PM Jul 6).

### Market Edge — **prod status (2026-07-06 night)**

| Area | Status |
| --- | --- |
| **Sources** | Finnhub + gov RSS + BBC/NPR + **Yahoo Finance** + **MarketWatch Top/Real-Time** (`20260706180000`) |
| **Edge** | `lounge-news-poll` redeployed test + prod (RSS default User-Agent) |
| **Voice** | Financial Juice / DeItaone brief; EDGAR filing feeds **off** (`20260706130000`) |
| **Reuters RSS** | **Do not add** — public `feeds.reuters.com` feeds are dead; use Yahoo/MW instead |

### Agent / CLI tooling (Jul 6)

- **`npm run db:query:test`** / **`db:query:production`** — serialized Supabase SQL (`scripts/supabase-db-query.mjs`). Use instead of parallel raw `supabase db query --linked` (pooler circuit breaker). Optional **`SUPABASE_DB_PASSWORD`** in `.env.supabase.*` for stable local probes only — **not** prod runtime.

### Shipped client (Vercel `main` → **edgetilt.com**)

Includes through **`b658647c`**: Bot portal async odds queue + outcome toasts, Coffee portal label, Scott calendar helper text. Prior: chart Add to post confirm (`014b3d4d`), deploy banner 20s, iOS post detail composer fix, bot reply on any post (`48d739db` + prod **`20260704220000`**).

### Prod SQL applied (Jul 2026 Scott + Market Edge — manual pattern)

Key migrations through **`20260706180000`** on **both** test + prod (verify via function/source rows, not only `schema_migrations`):

`20260704230000`–`20260704240000` (cron) · `20260705060000`–`20260706170000` (portal queue, slug fix, pg_net result, invoke `force`) · `20260706180000` (Yahoo/MW RSS)

### Next priorities (ordered)

1. **Scott morning Coffee smoke** — prod **~6–8am PT**; grep `net._http_response` + `lounge_bot_publish_log` for `coffee_covers`
2. **Scott `poll_edges` smoke** — confirm edge / line move / arb / sharp / live alerts publish (not just Coffee)
3. **Unschedule** leftover Coffee force-test cron on prod
4. **Market Edge smoke** — Poll now; confirm Yahoo/MW items score + publish
5. **Stripe prod smoke** / Starter weekly cron — **`docs/stripe-billing-test-to-prod-handoff.md`**

### AP guides (ongoing workflow)

**Source of truth:** Supabase **`guides.content_markdown`** + R2 heroes. **Editor:** **`/slot-guide-form`** on **`edgetilt.com`**.

**Prod = prod for agents:** Never ingest/batch-run to **`jtjgtucumuoswnbauxry`** unless Ryan names the slug/batch.

### Parked

- Thread compose iOS keyboard / options bar overlap after media pick
- Feed UI +EV badge (caption prefix only today)
- Reuters wire (needs LSEG/API license — no free RSS)
