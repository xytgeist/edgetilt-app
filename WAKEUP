# Morning handoff — how to greet Theo

**Ryan** — use this **after a restart or any new Cursor chat** so the session starts pointed the right way. Theo does not remember yesterday; **the repo and `AGENTS.md` are the source of truth.**

**Do not trim for size:** **`docs/test-buildout-backlog.md`** (test SQL, smoke, Update log) and **`docs/production-rollout-checklist.md`** (prod replay) hold everything that must survive across sessions. This file is **ephemeral** session context only.

## What you do (quick)

1. **Skim** the bracketed lines in **Copy into chat** below (branch, Supabase, goal).
2. **Optional:** Type a short hello in your own words on the first line (e.g. *Good morning Theo — pick up from WAKEUP Aug 13 handoff.*).
3. **Copy** from **“The agent should treat…”** through the last line you need, then **paste into chat**.

That is the whole ritual: **you set the rails; Theo reads `AGENTS.md` and the docs you name.**

---

## Copy into chat

The agent should treat the following as **standing instructions for this session** (full wording and examples live at the top of **`AGENTS.md`**):

- **Names:** You are **Theo** (human-style assistant name). The maintainer is **Ryan** — address him by name; do **not** call him “the user” or “user.” You are Ryan's teammate and friend. Ryan is the CEO and you are the CTO of a company building **Edge** / **EdgeTilt**, a social platform for risk takers (AP slots, poker, table games, day traders, investors, crypto, sports bettors, etc.). Banter and sarcastic trash talk are fine — especially when one of you does something stupid.
- **Voice:** Confident bestie energy; playful roasts OK when clearly affectionate — **always get serious** for security, correctness, risky merges, and keeping good notes/comments. Full wording: **`AGENTS.md`** → *People & naming* (`AGENT_RULE_RYAN_VOICE`).
- **Punctuation:** Never use em dashes (`—`). Prefer ellipses (`...`) for prose breaks. Cursor: **`.cursor/rules/no-em-dashes.mdc`**.
- **Cross-impact:** do not change existing code without checking it will not break other parts of the project (callers, shared components, dual-purpose logic, contracts).
- **Blast radius:** Before landing a change, tell Ryan **every other surface** it will affect (shared CSS classes, reused components, cross-feature imports) — not only the file or feature he named.
- **Questions:** If **Ryan** asks a question only, answer it — **no code unless he directs you to implement something.**
- **Design:** reason from **first principles** (concept, trade-offs, UX, maintainability, performance, real-world fit). See **`AGENTS.md`** → *Architecture and design reasoning*.
- **Light / dark UI:** If Ryan says **light mode only** or **dark mode only**, scope the change to that mode only — do not leak into the other unless he asked or you confirm first. See **`AGENTS.md`** → *LIGHT MODE vs DARK MODE* (`AGENT_RULE_THEME_MODE_ONLY`).
- **Supabase default:** **test** (`kcosfvmreeiosdjdzycb`) unless Ryan explicitly asks for **prod** (`jtjgtucumuoswnbauxry`).
- **Git (test):** After implementation work, **commit and push to `test` on your own** — Ryan pre-approved; do not ask every time. Still wait for explicit OK for **main**, prod deploy, or prod Supabase. See **`AGENTS.md`** → *GIT: COMMIT AND PUSH TO test* (`AGENT_RULE_COMMIT_PUSH_TEST`).

---

**Branch:** `main` / `test` aligned at **`ae7616d0`** (2026-08-19)

**Supabase (do not swap these):**

| | Project ref | Domain |
| --- | --- | --- |
| **Production** | **`jtjgtucumuoswnbauxry`** | **`edgetilt.com`** |
| **Test sandbox** | **`kcosfvmreeiosdjdzycb`** | **`lvslotpro.com`** |

**Goal:** _At home on the main PC: set up a **self-hosted GitHub Actions runner** so poker catalog sync scrapes MTTDB from residential egress (not `ubuntu-latest`). Catalog was manually refilled 2026-08-19 after GHA failed again. Buffalo Diamond calc stays grayed out until resets verified._

**Start by reading:** `AGENTS.md`, then this **Pick up here (2026-08-19)** section, **`docs/test-buildout-backlog.md`** Update log (**2026-08-19** poker catalog + Buffalo Diamond lines).

**Optional add-ons** (delete lines you do not need)

- Session focus: _[poker catalog self-hosted runner on main home PC]_
- No prod writes / no commits / PR only: _[say which]_
- Context: _[e.g. "see WAKEUP Pick up here Aug 19"]_

---

## Pick up here (Aug 2026 ... session 2026-08-19)

**Computer switch handoff (home main PC).** Tips **`main`** / **`test`** @ **`ae7616d0`**. Pull on the main machine, paste **Copy into chat** above, continue from **On deck #1 (self-hosted runner)**.

### Latest commits (`main` / `test` @ **`ae7616d0`**)

| Commit | What |
| --- | --- |
| **`ae7616d0`** | Disable Buffalo Diamond calc (hub + guides) until meter resets verified |
| **`7ec1bfe1`** | Prior prod tip: Diamond reset floors + Extreme $7 + Scott soccer labels (code) |
| Earlier Aug 19 | Scott Sharpe soccer shortDisplayName fix; Diamond/Extreme reset calibration |

### Shipped / ops this arc (need home follow-up)

| Area | Detail |
| --- | --- |
| **Buffalo Diamond** | Temporarily disabled via **`TEMPORARILY_DISABLED_CALCULATOR_KEYS`** (`buffalo-diamond` covers Diamond + Extreme). Re-enable by removing that key when resets are squared away. On **prod + test** @ **`ae7616d0`**. |
| **Poker catalog (prod)** | GHA cron failed **2/2** on MTTDB: shell HTML without embedded lobby JSON (CF bot score on Actions IPs). Manual local **`npm run poker:catalog:sync:production`** (2026-08-19 evening): **2273** upserted (MTTDB live **535** / online **582**, ClubWPT **70**). Catalog healthy again until next cron. |
| **Diagnosis** | Wrong **where** the job runs (`ubuntu-latest`), not a broken parser. Same script from residential egress works. |

### On deck #1 ... Poker catalog self-hosted runner (DO THIS AT HOME)

**Decision (Ryan + Theo 2026-08-19):** Stop relying on GitHub-hosted runners for MTTDB scrape. Schedule stays in GitHub; **job must execute on residential egress**.

**Recommended:** self-hosted Actions runner on the **main home PC** (always-on / usually-on preferred).

#### Ryan steps on the main PC

1. Pull repo (`test` or `main` @ **`ae7616d0`+**). Confirm **Node 22** installed.
2. GitHub → this repo → **Settings → Actions → Runners → New self-hosted runner** → **Windows**.
3. Download/configure in a durable folder (e.g. `C:\actionsgetilt-actions-runner`). Complete the token configure step GitHub shows.
4. Install as a **Windows service** so it survives reboot; leave service running.
5. Confirm runner shows **Idle** (green) in GitHub Settings → Runners.

#### Theo / repo steps (after runner is Idle)

1. Patch **`.github/workflows/poker-catalog-sync-production.yml`**: change `runs-on: ubuntu-latest` → `runs-on: [self-hosted, Windows, X64]` (match labels on the registered runner).
2. Commit + push **`test`**, then Ryan asks for **`main`** promote so the workflow file is live on the default branch Actions uses.
3. Smoke: **Actions → Poker catalog sync (production) → Run workflow**. Expect MTTDB online/live ≫ 0 (like the manual 582/535), not the shell-HTML failure.
4. Cron (`0 6 */3 * *` UTC) keeps firing from GitHub’s scheduler; execution is on the home PC. PC must be **on + online + runner service up** at cron time (or job waits/misses).

**Secrets:** keep using repo secrets `SUPABASE_URL_PRODUCTION` + `SUPABASE_SERVICE_ROLE_KEY_PRODUCTION` (no need to put prod keys in Task Scheduler).

**Fallback if runner is a pain:** Windows Task Scheduler every ~3 days → `git pull` + `npm ci` + `npm run poker:catalog:sync:production` with local **`.env.supabase.production`**. Worse for secrets/ops visibility; fine as interim.

**Do not:** mark sync green on regional JSON alone; re-run the same job on `ubuntu-latest` and expect CF to relent; put prod service-role in chat.

### Key files (grep anchors)

| Topic | Files |
| --- | --- |
| Catalog sync workflow | **`.github/workflows/poker-catalog-sync-production.yml`** |
| MTTDB scrape | **`scripts/lib/mttdbCatalogFetch.mjs`**, **`scripts/sync-poker-tournament-catalog.mjs`** |
| Buffalo Diamond disable | **`src/features/calculators/calculatorAccess.js`** (`TEMPORARILY_DISABLED_CALCULATOR_KEYS`) |
| Edge Monitor heartbeat | **`docs/edge-monitor-roadmap.md`** (poker catalog stale >4 days) |

### Also parked (still true)

- Buffalo Diamond / Extreme meter reset tables still incomplete (calc grayed out on purpose)
- Scott Sharpe soccer fix is on **`main`** code; **prod Edge** `lounge-odds-ingest` + `lounge-odds-poll` may still need redeploy if not done after **`7ec1bfe1`**
- Twilio **From number** for guest SMS on prod
- Poker Stable smoke checklist / older backlog items
- Thread compose iOS keyboard overlap; feed +EV badge; Reuters wire license

### Infra notes (still true)

- **Prod = prod** for guides ingest / Edge / SQL ... need Ryan's explicit ask
- MTTDB is behind Cloudflare; GHA datacenter egress will keep failing until runner/proxy change
- Prefer **`scripts/.tmp-*`** for scratch; do not commit **`scripts/tmp-debug-egress.mjs`**

### AP guides (ongoing workflow)

**Source of truth:** Supabase **`guides.content_markdown`** + R2 heroes. **Editor:** **`/slot-guide-form`** on **`edgetilt.com`**.

**Prod = prod for agents:** Never ingest/batch-run to **`jtjgtucumuoswnbauxry`** unless Ryan names the slug/batch.

---

## Cursor model switch guide (Ryan quick ref)

Standing advice ... not session-ephemeral. Start with **Grok** or **Composer**; switch when stuck or the job type changes.

### Quick pick

| Situation | Use |
| --- | --- |
| Ship a feature, fix a bug, deploy, update docs | **Grok** or **Composer 2.5** |
| Pure multi-file edits / “just change the code” | **Composer 2.5** |
| Hard architecture / subtle correctness / “why is this wrong?” | **Opus** |
| Balanced coding + reasoning, default daily driver | **Sonnet** |
| Product copy, UX wording, creative exploration | **Fable** |
| Structured analysis, tables, careful review | **Terra** |

### EdgeTilt cheat sheet

| Task | Model |
| --- | --- |
| Bot portal UI, Lounge CSS, small Edge Function tweak | Grok / Composer |
| Poker Stable settle / RLS / commit sync | Grok first; **Opus** if RPC/RLS cursed |
| Scott odds / live poll / cron / SQL chain | Grok first; **Opus** if cron/RLS/Edge auth is cursed |
| Freemium / RLS / entitlements design | **Opus** or Sonnet |
| AP guide copy / voice prompts | Fable or Grok |
| “Update all continuity docs” | Grok |
| Security / production cutover review | **Opus** (or Security Review subagent) |

### Rule of thumb

1. Start with **Grok** or **Composer**.
2. If you’re spinning or the design has real tradeoffs → **Opus**.
3. If you need pretty words → **Fable**.
4. Don’t model-hop mid-tiny-fix unless you’re stuck.
