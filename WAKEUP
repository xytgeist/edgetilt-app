# Morning handoff — how to greet Theo

**Ryan** — use this **after a restart or any new Cursor chat** so the session starts pointed the right way. Theo does not remember yesterday; **the repo and `AGENTS.md` are the source of truth.**

**Do not trim for size:** **`docs/test-buildout-backlog.md`** (test SQL, smoke, Update log) and **`docs/production-rollout-checklist.md`** (prod replay) hold everything that must survive across sessions. This file is **ephemeral** session context only.

## What you do (quick)

1. **Skim** the bracketed lines in **Copy into chat** below (branch, Supabase, goal).
2. **Optional:** Type a short hello in your own words on the first line (e.g. *Good morning Theo — smoke quote repost avatar on iPhone PWA.*).
3. **Copy** from **“The agent should treat…”** through the last line you need, then **paste into chat**.

That is the whole ritual: **you set the rails; Theo reads `AGENTS.md` and the docs you name.**

---

## Copy into chat

The agent should treat the following as **standing instructions for this session** (full wording and examples live at the top of **`AGENTS.md`**):

- **Names:** You are **Theo** (human-style assistant name). The maintainer is **Ryan** — address him by name; do **not** call him “the user” or “user.” You are Ryan's teammate and friend. Ryan is the CEO and you are the CTO of a company building **Edge** / **EdgeTilt**, a social platform for risk takers (AP slots, poker, table games, day traders, investors, crypto, sports bettors, etc.). Banter and sarcastic trash talk are fine — especially when one of you does something stupid.
- **Voice:** Confident bestie energy; playful roasts OK when clearly affectionate — **always get serious** for security, correctness, risky merges, and keeping good notes/comments. Full wording: **`AGENTS.md`** → *People & naming* (`AGENT_RULE_RYAN_VOICE`).
- **Cross-impact:** do not change existing code without checking it will not break other parts of the project (callers, shared components, dual-purpose logic, contracts).
- **Blast radius:** Before landing a change, tell Ryan **every other surface** it will affect (shared CSS classes, reused components, cross-feature imports) — not only the file or feature he named.
- **Questions:** If **Ryan** asks a question only, answer it — **no code unless he directs you to implement something.**
- **Design:** reason from **first principles** (concept, trade-offs, UX, maintainability, performance, real-world fit). See **`AGENTS.md`** → *Architecture and design reasoning*.
- **Light / dark UI:** If Ryan says **light mode only** or **dark mode only**, scope the change to that mode only — do not leak into the other unless he asked or you confirm first. See **`AGENTS.md`** → *LIGHT MODE vs DARK MODE* (`AGENT_RULE_THEME_MODE_ONLY`).
- **Supabase default:** **test** (`kcosfvmreeiosdjdzycb`) unless Ryan explicitly asks for **prod** (`jtjgtucumuoswnbauxry`).

---

**Branch:** `main` / `test` aligned at **`bf469ec3`** (2026-07-26)

**Supabase (do not swap these):**

| | Project ref | Domain |
| --- | --- | --- |
| **Production** | **`jtjgtucumuoswnbauxry`** | **`edgetilt.com`** |
| **Test sandbox** | **`kcosfvmreeiosdjdzycb`** | **`lvslotpro.com`** |

**Goal:** _Lounge feed UX polish batch shipped (carousel bleed, lightbox snap, quote inset avatar). Ryan smoke on iPhone PWA; Scott/bot work may continue in parallel._

**Start by reading:** `AGENTS.md`, then **`docs/test-buildout-backlog.md`** Update log tail (**2026-07-26** feed UX + Scott rows). Lounge media: **`docs/frontend-architecture.md`** → **`lounge/`** row.

**Optional add-ons** (delete lines you do not need)

- Session focus: _[e.g. smoke multi-image carousel on post detail + thread hierarchy; verify quote repost inset avatar]_
- No prod writes / no commits / PR only: _[say which]_
- Context: _[e.g. "see backlog Update log Jul 26 feed carousel batch"]_

---

## Cursor model switch guide (Ryan quick ref)

Standing advice ... not session-ephemeral. Start with **Grok** or **Composer**; switch when stuck or the job type changes.

### Quick pick

| Situation | Use |
| --- | --- |
| Ship a feature, fix a bug, deploy, update docs | **Grok** or **Composer 2.5** |
| Pure multi-file edits / “just change the code” | **Composer 2.5** |
| Hard architecture / subtle correctness / “why is this wrong?” | **Opus** |
| Balanced coding + reasoning, default daily driver | **Sonnet** |
| Product copy, UX wording, creative exploration | **Fable** |
| Structured analysis, tables, careful review | **Terra** |

### When to switch (one-liners)

- **Stay on Grok** when you want: find it → fix it → commit/push/deploy → update `WAKEUP` / backlog. Fast agent loops.
- **Switch to Composer 2.5** when the job is mostly mechanical repo surgery and you don’t need much product chat.
- **Switch to Opus** when you’re stuck after 1–2 attempts, the blast radius is scary, or the bug is “impossible” / cross-system.
- **Switch to Sonnet** when you want a steady middle ground: solid code + clear reasoning without Opus weight.
- **Switch to Fable** for voice, landing copy, bot persona wording, marketing-ish text.
- **Switch to Terra** for audits, checklists, migration inventories, “compare A vs B carefully.”

### EdgeTilt cheat sheet

| Task | Model |
| --- | --- |
| Bot portal UI, Lounge CSS, small Edge Function tweak | Grok / Composer |
| Scott odds / live poll / cron / SQL chain | Grok first; **Opus** if cron/RLS/Edge auth is cursed |
| X ingest / bearer / oEmbed weirdness | Grok |
| Freemium / RLS / entitlements design | **Opus** or Sonnet |
| AP guide copy / voice prompts | Fable or Grok |
| “Update all continuity docs” | Grok |
| Security / production cutover review | **Opus** (or Security Review subagent) |
| PR babysit / CI loop | Composer or Grok |

### Rule of thumb

1. Start with **Grok** or **Composer**.
2. If you’re spinning or the design has real tradeoffs → **Opus**.
3. If you need pretty words → **Fable**.
4. Don’t model-hop mid-tiny-fix unless you’re stuck.

---

## Pick up here (Jul 2026 ... session 2026-07-26)

### Latest commits (`main` / `test` @ **`bf469ec3`**)

| Commit | What |
| --- | --- |
| **`bf469ec3`** | Quote repost inset: author avatar beside meta row (X-style) |
| **`b6e6914a`** | Feed post row: remove `active:` background flash during scroll (iOS) |
| **`6a2b67c0`** | Thread hierarchy: carousel full-bleed only on **focus** row, not ancestors |
| **`a0972b9a`** | Profile sheet: fix horizontal pan from carousel `100vw` bleed overflow |
| **`2a246548`** | Hide scrollbars on feed carousels + lightbox pager (`index.css`) |
| **`64c6f5ea`** | Image lightbox: snap paging (not feed free-scroll); **`useLoungeLightboxCarouselSnap.js`** |
| **`fc929dc1`** | Post/comment detail: remove separator under title bar |
| **`23594dd8`** | Post detail: fix horizontal pan from carousel bleed (`overflow-x-hidden`) |
| **`f8483129`** | Extend horizontal image carousel to detail + comment media |
| **`dedc699f`** | PWA auth restore hardening on cold boot / resume |
| **`7c90f91a`** | Feed carousel rubber-band bounce at scroll limits |
| **`ab823f86`** | Restore vertical feed scroll over lounge media (axis lock) |

### Lounge feed carousel + bleed (shipped Jul 26)

| Area | Detail |
| --- | --- |
| **Surfaces** | Multi-image horizontal carousels on feed posts, post detail, comment rows, profile replies |
| **Axis lock** | **`useLoungeFeedCarouselAxisLock`** ... vertical feed scroll wins except on carousel strip |
| **Full bleed** | **`loungeFeedImageAttachment.js`** → **`loungeFeedCarouselFullBleed()`** ... feed posts + comment rows with **`captionColumnMedia={true}`** only |
| **Hierarchy** | **`LoungePostDetailCommentHierarchy`** / **`LoungePostThreadPartsHierarchy`**: ancestors **`captionColumnMedia={false}`**; focus/last row **`true`** |
| **Profile replies** | **`ProfileReplyRow`**: OP media in thread uses **`variant="detail"`** + **`captionColumnMedia={false}`** |
| **Horizontal pan bug** | **`overflow-y-auto`** scroll roots implicitly allow horizontal overflow from **`100vw`** bleed children ... fixed on post detail scroll ref + profile body scroll ref |

### Lightbox vs feed carousel (shipped Jul 26)

| Area | Detail |
| --- | --- |
| **Feed carousel** | Free horizontal scroll + axis lock + hidden scrollbar |
| **Lightbox pager** | **`useLoungeLightboxCarouselSnap.js`** + CSS **`snap-x snap-mandatory`** ... page between images, not free-scroll |
| **Swipe dismiss** | **`loungeLightboxSwipeDismiss.js`** skips pointer capture when touch starts on **`[data-lounge-lightbox-carousel]`** |

### Quote repost inset avatar (shipped Jul 26 @ **`bf469ec3`**)

| Area | Detail |
| --- | --- |
| **Component** | **`LoungeQuoteRepostEmbedAuthorMeta.jsx`** ... 20px avatar left of name/badges/handle/time |
| **Constant** | **`LOUNGE_QUOTE_EMBED_AVATAR_CLASS`** in **`loungeFeedAvatar.js`** |
| **Blast radius** | Feed quote embeds, post detail, quote composer preview, comment quote insets, locked fan-only embed shell |
| **Smoke** | Quote repost with inset OP ... avatar visible + tappable to profile (where **`onDisplayNameClick`** wired) |

### Scott / bots (parallel Jul 26 backlog ... not this chat's commits)

Heavy Jul 26 **`test-buildout-backlog.md`** rows: Scott alert thresholds, Situational Lean, Coffee voice, sub-chat dedupe, live pick quality, sport labels, bot **`feed_visible_at`** hotfix, **`lounge-odds-poll`** BOOT_ERROR fix. See backlog Update log **2026-07-26** Scott rows. Edge redeploys may still need Ryan smoke.

### Shipped client (Vercel `main` → **edgetilt.com**)

Through **`bf469ec3`**: full feed UX polish batch above. **No new SQL** in this batch. **No Edge redeploy** required for carousel/lightbox/avatar fixes.

### Next priorities (ordered)

1. **Ryan smoke (iPhone PWA):** multi-image post on feed → swipe carousel → open detail → no horizontal page pan; thread hierarchy ancestors stay inset, focus row bleeds; profile sheet same; lightbox snap between images; quote repost inset shows avatar
2. **Scott alert smoke** ... if continuing bot work: portal **Run alert now** + Coffee morning check (see backlog Jul 26 Scott rows)
3. **Staged video publish** ... prod smoke still open from **2026-07-25** backlog row if not yet signed off

### Key files (grep anchors)

| Topic | Files |
| --- | --- |
| Carousel bleed | **`loungeFeedImageAttachment.js`**, **`LoungePostFeedMedia.jsx`**, **`LoungeCommentCard.jsx`** |
| Horizontal pan | **`SocialFeed.jsx`** (`loungePostDetailScrollRef`), **`LoungeProfileFullScreen.jsx`** (`profileBodyScrollRef`) |
| Lightbox snap | **`useLoungeLightboxCarouselSnap.js`**, **`LoungeInlineMediaUrl.jsx`**, **`index.css`** |
| Quote avatar | **`LoungeQuoteRepostEmbedAuthorMeta.jsx`**, **`LoungeQuoteRepostEmbeddedOriginal.jsx`** |
| Feed row flash | **`loungeFeedAvatar.js`** (`LOUNGE_FEED_POST_ROW_CLASS`) |

### AP guides (ongoing workflow)

**Source of truth:** Supabase **`guides.content_markdown`** + R2 heroes. **Editor:** **`/slot-guide-form`** on **`edgetilt.com`**.

**Prod = prod for agents:** Never ingest/batch-run to **`jtjgtucumuoswnbauxry`** unless Ryan names the slug/batch.

### Parked

- Thread compose iOS keyboard / options bar overlap after media pick
- Feed UI +EV badge (caption prefix only today)
- Stream + still images in one carousel (upload order) ... **not scheduled**
- Reuters wire (needs LSEG/API license)
