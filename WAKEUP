# Morning handoff — how to greet Theo

**Ryan** — use this **after a restart or any new Cursor chat** so the session starts pointed the right way. Theo does not remember yesterday; **the repo and `AGENTS.md` are the source of truth.**

**Do not trim for size:** **`docs/test-buildout-backlog.md`** (test SQL, smoke, Update log) and **`docs/production-rollout-checklist.md`** (prod replay) hold everything that must survive across sessions. This file is **ephemeral** session context only.

## What you do (quick)

1. **Skim** the bracketed lines in **Copy into chat** below (branch, Supabase, goal).
2. **Optional:** Type a short hello in your own words on the first line (e.g. *Good morning Theo — pick up from WAKEUP Aug 13 handoff.*).
3. **Copy** from **“The agent should treat…”** through the last line you need, then **paste into chat**.

That is the whole ritual: **you set the rails; Theo reads `AGENTS.md` and the docs you name.**

---

## Copy into chat

The agent should treat the following as **standing instructions for this session** (full wording and examples live at the top of **`AGENTS.md`**):

- **Names:** You are **Theo** (human-style assistant name). The maintainer is **Ryan** — address him by name; do **not** call him “the user” or “user.” You are Ryan's teammate and friend. Ryan is the CEO and you are the CTO of a company building **Edge** / **EdgeTilt**, a social platform for risk takers (AP slots, poker, table games, day traders, investors, crypto, sports bettors, etc.). Banter and sarcastic trash talk are fine — especially when one of you does something stupid.
- **Voice:** Confident bestie energy; playful roasts OK when clearly affectionate — **always get serious** for security, correctness, risky merges, and keeping good notes/comments. Full wording: **`AGENTS.md`** → *People & naming* (`AGENT_RULE_RYAN_VOICE`).
- **Punctuation:** Never use em dashes (`—`). Prefer ellipses (`...`) for prose breaks. Cursor: **`.cursor/rules/no-em-dashes.mdc`**.
- **Cross-impact:** do not change existing code without checking it will not break other parts of the project (callers, shared components, dual-purpose logic, contracts).
- **Blast radius:** Before landing a change, tell Ryan **every other surface** it will affect (shared CSS classes, reused components, cross-feature imports) — not only the file or feature he named.
- **Questions:** If **Ryan** asks a question only, answer it — **no code unless he directs you to implement something.**
- **Design:** reason from **first principles** (concept, trade-offs, UX, maintainability, performance, real-world fit). See **`AGENTS.md`** → *Architecture and design reasoning*.
- **Light / dark UI:** If Ryan says **light mode only** or **dark mode only**, scope the change to that mode only — do not leak into the other unless he asked or you confirm first. See **`AGENTS.md`** → *LIGHT MODE vs DARK MODE* (`AGENT_RULE_THEME_MODE_ONLY`).
- **Supabase default:** **test** (`kcosfvmreeiosdjdzycb`) unless Ryan explicitly asks for **prod** (`jtjgtucumuoswnbauxry`).
- **Git (test):** After implementation work, **commit and push to `test` on your own** — Ryan pre-approved; do not ask every time. Still wait for explicit OK for **main**, prod deploy, or prod Supabase. See **`AGENTS.md`** → *GIT: COMMIT AND PUSH TO test* (`AGENT_RULE_COMMIT_PUSH_TEST`).

---

**Branch:** `main` / `test` aligned at **`2ca2e9d1`** (2026-08-13)

**Supabase (do not swap these):**

| | Project ref | Domain |
| --- | --- | --- |
| **Production** | **`jtjgtucumuoswnbauxry`** | **`edgetilt.com`** |
| **Test sandbox** | **`kcosfvmreeiosdjdzycb`** | **`lvslotpro.com`** |

**Goal:** _Lounge image lightbox polish shipped to prod (swipe dismiss + Android frost pills). `/patches` killed on prod. Poker catalog refill done manually after GHA 403. Next: optional harden catalog cron egress; otherwise pick Ryan's next product focus._

**Start by reading:** `AGENTS.md`, then this **Pick up here (2026-08-13)** section, **`docs/test-buildout-backlog.md`** Update log (**2026-08-12**–**2026-08-13**), **`docs/frontend-architecture.md`** → **`lounge/`** (image lightbox).

**Optional add-ons** (delete lines you do not need)

- Session focus: _[e.g. poker catalog Actions 403 long-term fix; Lounge lightbox follow-ups]_
- No prod writes / no commits / PR only: _[say which]_
- Context: _[e.g. "see WAKEUP Pick up here Aug 13"]_

---

## Pick up here (Aug 2026 ... session 2026-08-13)

**Computer switch handoff.** Tips **`main`** / **`test`** @ **`2ca2e9d1`**. Clone/pull on the new machine, paste **Copy into chat** above, continue from **On deck**.

### Latest commits (`main` / `test` @ **`2ca2e9d1`**)

| Commit | What |
| --- | --- |
| **`2ca2e9d1`** | Remove **`edgetilt.com/patches`** merch wall (HTML, assets, Vercel rewrite, sitemap, build script) |
| **`db41ae5b`** | Harden MTTDB lobby fetch (session warm-up, browser headers, 403/429 retries) |
| **`1e0e5445`** | Scale lightbox frost pills on skinny Android (`cqi` clamp + compact `w-full`) |
| **`c3ec7bcd`** | Image lightbox swipe dismiss on full chrome+media shell (caption-heavy / tall posts) |
| **`3532854a`** | Lightbox captions: tap expand 3→8 lines; no Show more / no detail nav |
| Earlier same arc | Ambient backdrop restore, tall edge-to-edge media, frost 15%, like chip `text-lv-red`, caption clamp |

Promote tip: **`2ca2e9d1`** (already on **main** + **test**). Vercel **`edgetilt.com`** / **`lvslotpro.com`** after deploy.

### Shipped this session (Lounge image lightbox + ops)

| Area | Detail |
| --- | --- |
| **Swipe dismiss** | Handlers on full lightbox shell with **capture**; caption allows dismiss (`data-lounge-lightbox-caption`); single-image **`touch-none`** (Stream-style). Fixed Fat Cat (short + long caption/cashtags) + Dean tall portrait on Android/prod |
| **Frost pills (Android)** | Fixed `h-10` / `5.5rem` overflowed skinny viewports. Now CSS vars from row **`cqi`**; compact chrome **`w-full`**. Ryan sign-off: swipe + buttons **fixed on Android** |
| **Caption** | 3-line clamp → tap expands 8-line scroll; no Show more; no navigate-to-detail |
| **Ambient / tall media** | Ambient not gated on CF Image Resizing (Free zone `/cdn-cgi/image/` 404). Tall slides edge-to-edge; short band-centered |
| **`/patches`** | Removed from prod + test (SEO docs + backlog updated) |
| **Poker catalog (prod)** | Scheduled Actions failed **HTTP 403** on MTTDB (CF blocks GHA egress). Manual **`npm run poker:catalog:sync:production`** from Ryan's machine: **2382** upserted (MTTDB online **586** / live **542**, ClubWPT **58**, CoinPoker **114**). Cron still **`0 6 */3 * *`** (~every 3 days UTC) |

### Key files (grep anchors)

| Topic | Files |
| --- | --- |
| Image lightbox shell / FLIP | **`LoungeInlineMediaUrl.jsx`**, **`loungeLightboxFlip.js`** |
| Swipe dismiss | **`loungeLightboxSwipeDismiss.js`** |
| Pinch zoom | **`loungeLightboxImageZoom.js`** |
| Frost pills / chrome | **`LoungePostInteractionBar.jsx`**, **`LoungeStreamVideoLightboxChrome.jsx`**, **`src/index.css`** (`[data-lounge-lightbox-pill-row]`) |
| Caption expand | **`LoungeExpandableRichCaption.jsx`** |
| MTTDB scrape | **`scripts/lib/mttdbCatalogFetch.mjs`**, **`scripts/sync-poker-tournament-catalog.mjs`**, **`.github/workflows/poker-catalog-sync-production.yml`** |
| Stream iOS playback (untouched this arc; still sacred) | **`docs/lounge-stream-ios-playback.md`**, **`.cursor/rules/lounge-stream-ios-playback.mdc`** |

### Do not regress

- Like chip color must stay a real Tailwind token (**`text-lv-red`**), not arbitrary hex missing from bundle
- Ambient backdrop must **not** be gated on CF Image Resizing
- Tall media edge-to-edge; short band-centered
- Caption: ellipsis + tap-to-expand, not detail navigation
- Positive platform guards only (`IS_IOS` / `IS_ANDROID`, never `!IS_ANDROID`)

### On deck (ordered)

1. **Poker catalog cron 403 (optional long-term):** Actions egress still may hard-fail even with warm-up/retries. Options: re-run sync from residential IP when red; self-hosted runner; proxy secret; accept manual refill. Do **not** mark sync green on regional JSON alone (false-green already fixed).
2. **Ryan's next product focus** ... lightbox arc is closed unless new bugs appear.
3. Older open items still in backlog (Poker Stable smoke checklist, Twilio From on prod, guest syndicate Phase 1b, etc.) ... see **`docs/test-buildout-backlog.md`**.

### Infra notes (still true)

- **CF Image Resizing** off on Free zone for `media.lvslotpro.com` / media-test ... client resize opt-in **`VITE_LOUNGE_CF_IMAGE_RESIZE=true`** only
- **Prod = prod** for guides ingest / Edge / SQL ... need Ryan's explicit ask
- Root **`.tmp-*`** scratch cleaned (2026-08-13); prefer **`scripts/.tmp-*`** or delete after use

### AP guides (ongoing workflow)

**Source of truth:** Supabase **`guides.content_markdown`** + R2 heroes. **Editor:** **`/slot-guide-form`** on **`edgetilt.com`**.

**Prod = prod for agents:** Never ingest/batch-run to **`jtjgtucumuoswnbauxry`** unless Ryan names the slug/batch.

### Parked (still true)

- Thread compose iOS keyboard / options bar overlap after media pick
- Feed UI +EV badge (caption prefix only today)
- Stream + still images in one carousel (upload order) ... **not scheduled**
- Reuters wire (needs LSEG/API license)
- Twilio **From number** for guest SMS on prod (email works via Resend)
- Poker catalog GHA MTTDB scrape may keep 403'ing until egress changes

---

## Cursor model switch guide (Ryan quick ref)

Standing advice ... not session-ephemeral. Start with **Grok** or **Composer**; switch when stuck or the job type changes.

### Quick pick

| Situation | Use |
| --- | --- |
| Ship a feature, fix a bug, deploy, update docs | **Grok** or **Composer 2.5** |
| Pure multi-file edits / “just change the code” | **Composer 2.5** |
| Hard architecture / subtle correctness / “why is this wrong?” | **Opus** |
| Balanced coding + reasoning, default daily driver | **Sonnet** |
| Product copy, UX wording, creative exploration | **Fable** |
| Structured analysis, tables, careful review | **Terra** |

### EdgeTilt cheat sheet

| Task | Model |
| --- | --- |
| Bot portal UI, Lounge CSS, small Edge Function tweak | Grok / Composer |
| Poker Stable settle / RLS / commit sync | Grok first; **Opus** if RPC/RLS cursed |
| Scott odds / live poll / cron / SQL chain | Grok first; **Opus** if cron/RLS/Edge auth is cursed |
| Freemium / RLS / entitlements design | **Opus** or Sonnet |
| AP guide copy / voice prompts | Fable or Grok |
| “Update all continuity docs” | Grok |
| Security / production cutover review | **Opus** (or Security Review subagent) |

### Rule of thumb

1. Start with **Grok** or **Composer**.
2. If you’re spinning or the design has real tradeoffs → **Opus**.
3. If you need pretty words → **Fable**.
4. Don’t model-hop mid-tiny-fix unless you’re stuck.
