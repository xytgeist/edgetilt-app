# Morning handoff — how to greet Theo

**Ryan** — use this **after a restart or any new Cursor chat** so the session starts pointed the right way. Theo does not remember yesterday; **the repo and `AGENTS.md` are the source of truth.**

**Do not trim for size:** **`docs/test-buildout-backlog.md`** (test SQL, smoke, Update log) and **`docs/production-rollout-checklist.md`** (prod replay) hold everything that must survive across sessions. This file is **ephemeral** session context only.

## What you do (quick)

1. **Skim** the bracketed lines in **Copy into chat** below (branch, Supabase, goal).
2. **Optional:** Type a short hello in your own words on the first line (e.g. *Good morning Theo — smoke backer close settle commit on edgetilt.com.*).
3. **Copy** from **“The agent should treat…”** through the last line you need, then **paste into chat**.

That is the whole ritual: **you set the rails; Theo reads `AGENTS.md` and the docs you name.**

---

## Copy into chat

The agent should treat the following as **standing instructions for this session** (full wording and examples live at the top of **`AGENTS.md`**):

- **Names:** You are **Theo** (human-style assistant name). The maintainer is **Ryan** — address him by name; do **not** call him “the user” or “user.” You are Ryan's teammate and friend. Ryan is the CEO and you are the CTO of a company building **Edge** / **EdgeTilt**, a social platform for risk takers (AP slots, poker, table games, day traders, investors, crypto, sports bettors, etc.). Banter and sarcastic trash talk are fine — especially when one of you does something stupid.
- **Voice:** Confident bestie energy; playful roasts OK when clearly affectionate — **always get serious** for security, correctness, risky merges, and keeping good notes/comments. Full wording: **`AGENTS.md`** → *People & naming* (`AGENT_RULE_RYAN_VOICE`).
- **Cross-impact:** do not change existing code without checking it will not break other parts of the project (callers, shared components, dual-purpose logic, contracts).
- **Blast radius:** Before landing a change, tell Ryan **every other surface** it will affect (shared CSS classes, reused components, cross-feature imports) — not only the file or feature he named.
- **Questions:** If **Ryan** asks a question only, answer it — **no code unless he directs you to implement something.**
- **Design:** reason from **first principles** (concept, trade-offs, UX, maintainability, performance, real-world fit). See **`AGENTS.md`** → *Architecture and design reasoning*.
- **Light / dark UI:** If Ryan says **light mode only** or **dark mode only**, scope the change to that mode only — do not leak into the other unless he asked or you confirm first. See **`AGENTS.md`** → *LIGHT MODE vs DARK MODE* (`AGENT_RULE_THEME_MODE_ONLY`).
- **Supabase default:** **test** (`kcosfvmreeiosdjdzycb`) unless Ryan explicitly asks for **prod** (`jtjgtucumuoswnbauxry`).
- **Git (test):** After implementation work, **commit and push to `test` on your own** — Ryan pre-approved; do not ask every time. Still wait for explicit OK for **main**, prod deploy, or prod Supabase. See **`AGENTS.md`** → *GIT: COMMIT AND PUSH TO test* (`AGENT_RULE_COMMIT_PUSH_TEST`).

---

**Branch:** `main` / `test` aligned at **`4fb513ab`** (2026-08-05)

**Supabase (do not swap these):**

| | Project ref | Domain |
| --- | --- | --- |
| **Production** | **`jtjgtucumuoswnbauxry`** | **`edgetilt.com`** |
| **Test sandbox** | **`kcosfvmreeiosdjdzycb`** | **`lvslotpro.com`** |

**Goal:** _Poker Stable backer closed-horse + settle commit UX shipped to prod. Next: Ryan smoke on **edgetilt.com** (close settle inline Commit on horse Overview, archive revoked/closed carousel cards, no duplicate deal rows after revoke + Create Stake). Clean up Edge Lord test ghost deals if still in carousel._

**Start by reading:** `AGENTS.md`, then **`docs/poker-stable-spec.md`** (Update log **2026-08-04**–**2026-08-05**), **`docs/test-buildout-backlog.md`** Update log (**2026-08-05**), **`docs/production-rollout-checklist.md`** (Poker Stable backer carousel row @ **`4fb513ab`**).

**Optional add-ons** (delete lines you do not need)

- Session focus: _[e.g. archive ghost `$25/50 Test 3` carousel card on prod; smoke inline Commit after Lvslotgod close settle]_
- No prod writes / no commits / PR only: _[say which]_
- Context: _[e.g. "see WAKEUP Pick up here Aug 5 + poker-stable-spec Update log"]_

---

## Pick up here (Aug 2026 ... session 2026-08-05)

### Latest commits (`main` / `test` @ **`4fb513ab`**)

| Commit | What |
| --- | --- |
| **`4fb513ab`** | Inline backer settle **Commit** on horse deal **Overview** (`PokerStableCommitSyncPanel`); carousel **Review stake** opens deal detail, not stacked modal |
| **`d85883de`** | Backer **Create Stake after revoke** reopens same deal row (no duplicate); label disambiguation (`· Aug 3`); optimistic archive patch |
| **`1b4b953e`** | Backer archive on **revoked** stakes (declined slices); **`PokerStableClosedHorseSheet`** Review + **Delete** |
| **`db01df4b`** | TDZ hotfix: `closedUnarchived` before use in horse carousel |
| **`b652f3b2`** | Backer closed-horse carousel, at-risk ROI, stakee commit-before-archive |
| **`e7314b94`** | Stakee settle/commit deep links → Bankroll; closed stake hero polish |

Promote tip for this Stable bundle: **`4fb513ab`**. Client + SQL on **main** / **test**; Vercel **`edgetilt.com`** after deploy.

### Shipped this session: Poker Stable backer closed horse + settle commit (test + **prod**)

| Area | Detail |
| --- | --- |
| **SQL (test + prod)** | **`20260805120000`** slice **`stable_archived_at`** + archive RPC; **`20260805130000`** archive includes **declined** slices on terminal deals (revoke path); **`20260805140000`** backer may delete slices on **revoked** deals for re-offer |
| **Duplicate deal bug (root cause)** | NOT UI double-render. **Create Stake after revoke** did a fresh **INSERT** (only blocked pending/active). Fix: **`requestBackingDeal`** reopens matching **revoked** deal (same label + guest/player), wipes slices, **`pending`**. Old ghost rows remain in DB until archived |
| **Closed horse carousel** | Revoked/closed/settled stakes stay in **Active horses** until backer **Archive stake** (slice-level **`stable_archived_at`**, parity with stakee Bankroll ARCHIVE) |
| **Review flows** | Carousel **Review** on revoked/declined → **`PokerStableClosedHorseSheet`** (Archive + Delete). Closed horse with player close settle → **Review stake** opens **`PokerStableDealDetailSheet`** Overview with inline **Commit** block |
| **Settle commit UX** | **`PokerStableCommitSyncPanel`** on deal **Overview** tab (roll, baseline, Stable backing bankroll on commit, **Commit**). Global **`PokerStableCommitSyncModal`** kept for Alerts/push deep links only (**`AppShell`**) |
| **Label disambiguation** | Duplicate stake labels show created date (**`$25/50 Test 3 · Aug 3`**) in carousel + Closed stakes list |
| **Canonical docs** | **`docs/poker-stable-spec.md`** Update log; backlog Update log **2026-08-05**; prod checklist row added |

### Prod data cleanup (Edge Lord test stakes ... Ryan manual)

Ryan was smoke-testing guest-player Create Stake deals on **edgetilt.com**. Before the revoke re-offer fix, duplicate rows existed for same label (e.g. two **`$25/50 Test 3`**):

| Deal | State |
| --- | --- |
| First Test 3 | Revoked + **archived** → **Closed stakes** |
| Second Test 3 (`c6219bf2-…`) | Revoked, **not archived** → still in carousel until **Review → Archive/Delete** |

Same pattern possible for Test 6 (case mismatch labels). After **`d85883de`**, revoke + Create Stake with same label should **reuse** one row going forward.

### On deck (ordered)

1. **Prod smoke (edgetilt.com):** Backer **Create Stake** → player accepts → session → player **close settle** → backer carousel **Needs attn** → **Review stake** → Overview shows inline **Close settlement** + **Commit** (no second modal) → **Archive stake** moves card to Closed stakes.
2. **Prod smoke:** Revoke stake → **Create Stake** same label → confirm **one** deal row (no ghost duplicate).
3. **Prod smoke:** Archive **revoked** stake (was failing **"Stake cannot be archived yet"** before **`20260805130000`**).
4. **Ryan cleanup:** Archive remaining unarchived revoked test carousel cards (especially second Test 3) or one-time SQL if Ryan asks.
5. **Poker Stable smoke checklist** (`/?tab=stable-smoke`) still open for full E2E sign-off.
6. **Phase 1b (not built):** guest syndicate co-backer claim on backer Create Stake — spec only.

### Key files (grep anchors)

| Topic | Files |
| --- | --- |
| Inline settle commit | **`PokerStableCommitSyncPanel.jsx`**, **`PokerStableDealDetailSheet.jsx`**, **`PokerStableCommitSyncModal.jsx`** (Alerts only) |
| Closed horse carousel | **`PokerStableHorseCarousel.jsx`**, **`PokerStableClosedHorseHeroBanner.jsx`**, **`PokerStableClosedHorseSheet.jsx`**, **`pokerStableBackerMath.js`** (`partitionBackerDeals`, `backerStableShowsClosedCarouselCard`) |
| Revoke re-offer | **`pokerStableApi.js`** (`requestBackingDeal`), migration **`20260805140000`** |
| Archive RPC | migration **`20260805130000`**, **`20260805120000`** |
| Stable screen | **`PokerStableScreen.jsx`** |
| Spec | **`docs/poker-stable-spec.md`** |

### Architecture reminders (backer vs stakee)

- **Player stake** = Poker Bankroll carousel; **Stable** = backer backing bankroll manager.
- Backer archive is **slice-level** (`stable_archived_at`), not deal-level like stakee (`stakee_bankroll_archived_at`).
- Stakee re-offer on revoked uses SQL **`poker_stable_apply_stakee_terms`**; backer re-offer now mirrors via client **`requestBackingDeal`** + slice delete RLS.
- **`partitionBackerDeals`**: each deal ID is either carousel **or** Closed stakes history, never both ... duplicate labels were **two deal rows**, not a partition bug.

### AP guides (ongoing workflow)

**Source of truth:** Supabase **`guides.content_markdown`** + R2 heroes. **Editor:** **`/slot-guide-form`** on **`edgetilt.com`**.

**Prod = prod for agents:** Never ingest/batch-run to **`jtjgtucumuoswnbauxry`** unless Ryan names the slug/batch.

### Parked (still true)

- Thread compose iOS keyboard / options bar overlap after media pick
- Feed UI +EV badge (caption prefix only today)
- Stream + still images in one carousel (upload order) ... **not scheduled**
- Reuters wire (needs LSEG/API license)
- Poker Online soft-event catalog (MTTDB or similar)
- Twilio **From number** for guest SMS on prod (email works via Resend)

---

## Cursor model switch guide (Ryan quick ref)

Standing advice ... not session-ephemeral. Start with **Grok** or **Composer**; switch when stuck or the job type changes.

### Quick pick

| Situation | Use |
| --- | --- |
| Ship a feature, fix a bug, deploy, update docs | **Grok** or **Composer 2.5** |
| Pure multi-file edits / “just change the code” | **Composer 2.5** |
| Hard architecture / subtle correctness / “why is this wrong?” | **Opus** |
| Balanced coding + reasoning, default daily driver | **Sonnet** |
| Product copy, UX wording, creative exploration | **Fable** |
| Structured analysis, tables, careful review | **Terra** |

### EdgeTilt cheat sheet

| Task | Model |
| --- | --- |
| Bot portal UI, Lounge CSS, small Edge Function tweak | Grok / Composer |
| Poker Stable settle / RLS / commit sync | Grok first; **Opus** if RPC/RLS cursed |
| Scott odds / live poll / cron / SQL chain | Grok first; **Opus** if cron/RLS/Edge auth is cursed |
| Freemium / RLS / entitlements design | **Opus** or Sonnet |
| AP guide copy / voice prompts | Fable or Grok |
| “Update all continuity docs” | Grok |
| Security / production cutover review | **Opus** (or Security Review subagent) |

### Rule of thumb

1. Start with **Grok** or **Composer**.
2. If you’re spinning or the design has real tradeoffs → **Opus**.
3. If you need pretty words → **Fable**.
4. Don’t model-hop mid-tiny-fix unless you’re stuck.
