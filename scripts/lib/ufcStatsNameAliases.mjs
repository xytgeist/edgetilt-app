/**
 * Roster name → UFC Stats listing name when they differ.
 * Keep small; prefer fixing fighter_name in DB when permanent.
 */
export const UFC_STATS_NAME_ALIASES = {
  'Ian Garry': 'Ian Machado Garry',
}
