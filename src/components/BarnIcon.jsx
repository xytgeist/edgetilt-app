import { barn } from '@lucide/lab'
import LucideLabIcon from './LucideLabIcon.jsx'

/** Lucide Lab barn (not in main lucide-react). */
export default function BarnIcon(props) {
  return <LucideLabIcon iconNode={barn} {...props} />
}
